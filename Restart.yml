---
- name: Restart Docker Service and Run Docker Compose
  hosts: all
  become: yes
  tasks:
    - name: Start Docker Service
      command: sudo docker compose up
      args:
        chdir: /home/ubuntu/easyTravel-Docker/
